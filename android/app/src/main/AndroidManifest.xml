<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
                     tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <application
        android:label="Liberty Shield Agent"
        android:allowBackup="false">
        <service
            android:name=".ShieldService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- exported="true" is required: the Android VPN framework must be able to bind
             to this service to manage the VPN lifecycle and display the system key icon.
             BIND_VPN_SERVICE permission restricts binding to the platform only. -->
        <service
            android:name=".vpn.ShieldVpnService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="connectedDevice"
            android:permission="android.permission.BIND_VPN_SERVICE">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

        <activity
            android:name=".LauncherActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".RuntimeDashboardActivity"
            android:exported="false"
            android:label="Runtime Dashboard" />

        <activity
            android:name=".BatterySetupActivity"
            android:exported="false"
            android:label="Battery Setup" />

        <activity
            android:name=".VpnPermissionActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- TEST MODE: two-phone harness — not for production -->
        <activity
            android:name=".test.TestModeActivity"
            android:exported="false"
            android:label="[TEST] Liberty Shield" />

        <service
            android:name=".test.TestModeService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>

</manifest>
